// En un principio esto no lo usamos, si la profesora se pone exigente con el tipado del codigo lo usamos, sino queda aca.
export interface Product {
  id: number;
  name: string;
  price: number;
  image: string;
}
